const authConfig = {
    providers: [
        {
            domain: "https://accepted-hamster-48.clerk.accounts.dev",
            applicationID: "convex",
        },
    ]
};

export default authConfig;